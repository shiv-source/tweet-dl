# Release workflow — triggered by pushing a version tag.
#
# Usage:
#   pnpm version patch        # 0.1.0 → 0.1.1  (bug fixes)
#   pnpm version minor        # 0.1.0 → 0.2.0  (new features)
#   pnpm version major        # 0.1.0 → 1.0.0  (breaking changes)
#
#   pnpm version prerelease --preid=rc   # 1.0.0 → 1.0.1-rc.0
#   pnpm version prerelease --preid=alpha # 1.0.0 → 1.0.1-alpha.0
#
#   git push --follow-tags
#
# Tag → npm dist-tag mapping:
#   v1.0.0            → latest   (npm install tweet-dl)
#   v1.0.1-rc.0       → rc       (npm install tweet-dl@rc)
#   v1.0.0-alpha.0    → alpha    (npm install tweet-dl@alpha)
#   v1.0.0-beta.1     → beta     (npm install tweet-dl@beta)
#
# Setup: add an NPM_TOKEN secret in GitHub repo Settings → Secrets.

name: Release

on:
  push:
    tags:
      # SemVer: major.minor.patch with optional -prerelease suffix
      - 'v[0-9]+.[0-9]+.[0-9]+*'

jobs:
  # ── Verify ──────────────────────────────────────────────
  # Runs lint, typecheck, and tests across Node 20, 22, 24.
  # Rejects the release if anything fails.
  # ────────────────────────────────────────────────────────
  verify:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [20.x, 22.x, 24.x]
    steps:
      - uses: actions/checkout@7.0.1

      - uses: pnpm/action-setup@4.1.0
        with:
          version: latest

      - uses: actions/setup-node@7.0.0
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'pnpm'

      - run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm run lint

      - name: Type check
        run: pnpm run typecheck

      - name: Test
        run: pnpm test

  # ── Build ───────────────────────────────────────────────
  # Compiles TypeScript and creates an npm tarball.
  # ────────────────────────────────────────────────────────
  build:
    needs: verify
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@7.0.1

      - uses: pnpm/action-setup@4.1.0
        with:
          version: latest

      - uses: actions/setup-node@7.0.0
        with:
          node-version: 22.x
          cache: 'pnpm'

      - run: pnpm install --frozen-lockfile

      - name: Build
        run: pnpm run build

      - name: Package
        run: pnpm pack

      - uses: actions/upload-artifact@7.0.1
        with:
          name: package
          retention-days: 1
          path: tweet-dl-*.tgz

  # ── Release ─────────────────────────────────────────────
  # Creates a GitHub Release with the packaged tarball.
  # prerelease: auto detects -alpha/-beta/-rc from the tag.
  # ────────────────────────────────────────────────────────
  release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@7.0.1

      - uses: actions/download-artifact@8.0.1
        with:
          name: package

      - name: Create GitHub Release
        uses: softprops/action-gh-release@3.0.2
        with:
          files: tweet-dl-*.tgz
          generate_release_notes: true
          prerelease: auto

  # ── Publish ──────────────────────────────────────────────
  # Publishes the package to the npm registry.
  # Requires an NPM_TOKEN secret in repo Settings → Secrets.
  # ────────────────────────────────────────────────────────
  publish:
    needs: release
    runs-on: ubuntu-latest
    permissions:
      id-token: write
    steps:
      - uses: actions/checkout@7.0.1

      - uses: pnpm/action-setup@4.1.0
        with:
          version: latest

      - uses: actions/setup-node@7.0.0
        with:
          node-version: 22.x
          registry-url: 'https://registry.npmjs.org'

      - uses: actions/download-artifact@8.0.1
        with:
          name: package

      - name: Detect npm tag
        id: npm_tag
        run: |
          TAG="${{ github.ref_name }}"
          VERSION="${TAG#v}"
          if [[ "$VERSION" == *-* ]]; then
            PRETAG="${VERSION#*-}"
            PRETAG="${PRETAG%%.*}"
            echo "tag=$PRETAG" >> "$GITHUB_OUTPUT"
          else
            echo "tag=latest" >> "$GITHUB_OUTPUT"
          fi

      - name: Publish to npm
        run: |
          mkdir -p /tmp/pkg
          tar xf tweet-dl-*.tgz --strip-components=1 -C /tmp/pkg
          cd /tmp/pkg
          pnpm publish --tag "${{ steps.npm_tag.outputs.tag }}" --access public --no-git-checks
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
